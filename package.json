{
  "name": "kanji-mindmap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/js/**/*.test.js",
    "serve": "python -m http.server 8000"
  }
}
